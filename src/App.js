import * as React from "react";

import { RoutesManager } from "./routes";

const App = () => {
  return (
    <div className="container">
      <RoutesManager />
    </div>
  );
};

export default App;
